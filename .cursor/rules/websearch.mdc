---
description: 
globs: 
alwaysApply: false
---
# 网络搜索专用规则

## 搜索策略
- **必须使用互联网搜索**：所有问题和查询都必须通过互联网搜索来获取答案
- **禁止本地代码搜索**：严格禁止使用 `codebase_search`、`grep_search`、`read_file` 等工具搜索当前项目的代码
- **禁止文件操作**：不得读取、编辑或创建项目中的任何文件

## 搜索行为准则
1. **优先使用 web_search 工具**：所有技术问题、概念解释、最佳实践等都应通过网络搜索获取最新信息
2. **多次搜索验证**：对于复杂问题，进行多次不同角度的搜索以获取全面信息
3. **搜索最新信息**：关注最新的技术文档、官方指南和社区最佳实践
4. **引用可信来源**：优先引用官方文档、知名技术博客、Stack Overflow、GitHub等可信来源

## 禁止操作
- ❌ 不得使用 `codebase_search` 搜索项目代码
- ❌ 不得使用 `grep_search` 查找项目文件
- ❌ 不得使用 `read_file` 读取项目文件
- ❌ 不得使用 `list_dir` 浏览项目目录
- ❌ 不得分析或引用当前项目的任何代码
- ❌ 不得访问本地文件系统中的代码

## 回答格式
在回答问题时应：
1. 明确说明信息来源于互联网搜索
2. 提供相关的网址链接（如有）
3. 注明信息的时效性
4. 给出完整、准确、实用的解决方案

## 示例触发词
当用户使用以下标识时启用此规则：
- `@websearch`
- `网络搜索`
- `互联网搜索`
- `在线查询`

记住：本规则的核心目标是确保所有答案都来自互联网，而不是本地项目代码。
